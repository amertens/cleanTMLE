% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_support.R
\name{support_surfaces}
\alias{support_surfaces}
\title{Prespecify the Simulated Outcome-Surface Family for simulate_support()}
\usage{
support_surfaces(
  confounding = c(0, 1, 2),
  modification = c(0, 1),
  complexity = "linear",
  effect = 0.05,
  base_rate = NULL
)
}
\arguments{
\item{confounding}{Numeric vector of confounding strengths (log-odds of the
baseline outcome per SD of the propensity direction). Default
\code{c(0, 1, 2)}.}

\item{modification}{Numeric vector of effect-modification strengths. At
\code{m}, the additive effect at propensity-direction value \code{s} is
\code{effect * (1 + m * s)}, so \code{m = 0} is a constant effect and at \code{m = 1}
the effect doubles one SD into the treated-typical region and vanishes
one SD into the control-typical region. Default \code{c(0, 1)}.}

\item{complexity}{Character vector, subset of \code{c("linear", "interaction")}.
\code{"interaction"} adds \code{0.5 * confounding * (s^2 - 1)} to the baseline
log-odds, curvature a main-terms Q model misses. Default \code{"linear"}.}

\item{effect}{Numeric; the additive risk difference at \code{s = 0}.
Default 0.05.}

\item{base_rate}{Numeric in (0, 1) or \code{NULL}; the marginal baseline
outcome rate the simulated surface is anchored to. \code{NULL} uses the
observed marginal outcome rate when the lock's outcome is readable
(a marginal summary, not the treatment-outcome association), else 0.10.}
}
\value{
An object of class \code{support_surfaces}: the grid specification.
}
\description{
The simulated outcome family spans three axes. The confounding axis scales
how strongly the outcome depends on the covariate direction that most
predicts treatment (the standardised logit of the fitted propensity), so
extrapolation error under non-overlap grows along it. The modification axis
makes the treatment effect vary along the same direction, so the ATE, ATT
and ATO genuinely differ and each estimator is judged against its own
estimand. The complexity axis optionally adds curvature (a squared term in
the propensity direction) that a main-terms outcome model cannot represent,
so Q misspecification is part of the stress. Positivity bias is
extrapolation error: it depends on how the outcome surface behaves where
only one arm is observed, which is exactly what this family varies.
}
\examples{
\dontrun{
support_surfaces()
support_surfaces(confounding = c(0, 0.5, 1, 2), modification = c(0, 0.5, 1))
}
}
\seealso{
\code{\link[=simulate_support]{simulate_support()}}
}
\keyword{internal}
