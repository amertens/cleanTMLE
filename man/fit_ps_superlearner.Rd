% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{fit_ps_superlearner}
\alias{fit_ps_superlearner}
\title{Fit Propensity Score Using SuperLearner}
\usage{
fit_ps_superlearner(lock, truncate = 0.01, cv_folds = 10L, cluster = NULL, ...)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link[=create_analysis_lock]{create_analysis_lock()}}.}

\item{...}{Additional arguments passed to \code{SuperLearner::SuperLearner()}.}
}
\value{
An object of class \code{ps_fit} containing propensity scores (\code{ps}),
the fitted SuperLearner object (\code{sl_fit}), and metadata.
}
\description{
Estimates the treatment propensity score P(A=1|W) using SuperLearner
ensemble learning. Only covariates (W) and treatment (A) are used;
the outcome is never accessed, so this step is safe at Stage 2a.
}
\details{
Requires the \pkg{SuperLearner} package.
}
\examples{
\dontrun{
dat  <- sim_func1(n = 500, seed = 1)
lock <- create_analysis_lock(dat, "treatment", "event_24",
                             c("age", "sex", "biomarker"), seed = 1)
ps_fit <- fit_ps_superlearner(lock)
}

}
