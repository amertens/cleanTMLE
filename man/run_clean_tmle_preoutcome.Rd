% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_tmle_staged.R
\name{run_clean_tmle_preoutcome}
\alias{run_clean_tmle_preoutcome}
\title{Pre-Outcome Pass of the Staged Clean-Room Workflow}
\usage{
run_clean_tmle_preoutcome(
  data,
  Avar,
  Yvar,
  covariates = NULL,
  learner_lib,
  tmle_candidates = NULL,
  selection_rule = "min_rmse",
  dq_scenarios = NULL,
  neg_control_outcomes = NULL,
  plasmode_reps = 50L,
  run_selection = TRUE,
  ps_method = c("superlearner", "glm"),
  cohort_thresholds = list(),
  balance_thresholds = list(),
  seed = 42L,
  allow_flag = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{Avar, Yvar}{Treatment and outcome column names.}

\item{covariates}{Character vector of covariates; if \code{NULL}, all numeric
columns other than the treatment, outcome, and common id/time columns.}

\item{learner_lib}{Pre-specified SuperLearner library.}

\item{tmle_candidates}{Optional list of TMLE candidates for the plasmode
loop; if \code{NULL}, the default candidate grid is used.}

\item{selection_rule}{Candidate-selection rule (see
\code{\link[=select_tmle_candidate]{select_tmle_candidate()}}).}

\item{dq_scenarios}{Optional DQ-stress scenarios (see
\code{\link[=run_plasmode_dq_stress]{run_plasmode_dq_stress()}}); when supplied, a DQ gate is recorded.}

\item{neg_control_outcomes}{Optional character vector of negative-control
outcome columns.}

\item{plasmode_reps}{Replicates for the plasmode loop.}

\item{run_selection}{Logical; run the outcome-blind candidate selection /
DQ stage. Default \code{TRUE}.}

\item{ps_method}{\code{"superlearner"} (default) or \code{"glm"} for the
propensity-score fit.}

\item{cohort_thresholds, balance_thresholds}{Named lists of thresholds passed
to \code{\link[=checkpoint_cohort_adequacy]{checkpoint_cohort_adequacy()}} and \code{\link[=checkpoint_balance]{checkpoint_balance()}}.}

\item{seed}{Integer seed recorded on the lock.}

\item{allow_flag}{Logical; whether a FLAG gate still authorises (default
\code{TRUE}).}

\item{verbose}{Logical; emit stage messages.}
}
\value{
A \code{clean_tmle_preoutcome} object with the (unauthorised) \code{lock},
the \code{audit}, the \code{gate} token, the \code{dossier}, and the design diagnostics.
}
\description{
Runs the outcome-blind stages of the workflow (cohort adequacy,
propensity-score balance, optional outcome-blind candidate selection and
data-quality stress test, optional negative controls) and assembles a
reviewer-facing dossier and a pre-outcome gate. The primary
treatment-outcome association is \strong{not} estimated and the returned lock is
left unauthorised: pass the result to \code{\link[=authorize_outcome_analysis]{authorize_outcome_analysis()}} and
then \code{\link[=run_clean_tmle_primary]{run_clean_tmle_primary()}} to read the outcome.
}
\seealso{
\code{\link[=run_clean_tmle_primary]{run_clean_tmle_primary()}}, \code{\link[=authorize_outcome_analysis]{authorize_outcome_analysis()}}
}
