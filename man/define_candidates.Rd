% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{define_candidates}
\alias{define_candidates}
\title{Define the Candidate Estimator Set}
\usage{
define_candidates(
  candidate_id = NULL,
  label = candidate_id,
  g_library = c("SL.glm"),
  q_library = NULL,
  truncation = 0.01,
  ...,
  grid = NULL
)
}
\arguments{
\item{candidate_id}{Character id for a single candidate (omit when
\code{grid} is given).}

\item{label}{Display label; defaults to the id.}

\item{g_library, q_library}{SuperLearner libraries for the treatment
and outcome nuisances; \code{q_library} defaults to \code{g_library}.}

\item{truncation}{Propensity truncation bound (or a named rule
resolved at fit time).}

\item{...}{Further specification fields (estimator, cv_scheme,
variance_method, discrete_sl, screener; see the candidate
internals).}

\item{grid}{A named list of grid axes: \code{truncations} (numeric
vector) and \code{libraries} (named list of character libraries), plus
optionally \code{estimators}, \code{cv_schemes}, \code{variance_methods},
\code{discrete_sl}, \code{screeners}, \code{max_candidates}.}
}
\value{
An object of class \code{ct_candidates}: a named list of
candidate specifications.
}
\description{
Constructs the prespecified TMLE candidate set: either a single
candidate (give \code{candidate_id} plus the specification arguments) or a
factorial grid (give \code{grid}, a named list of axes). Candidates are
fixed choices of nuisance libraries, truncation, and cross-fitting;
\code{\link[=stress_test]{stress_test()}} scores them on simulated outcomes and
\code{\link[=select_candidate]{select_candidate()}} applies the locked selection rule.
}
\examples{
cands <- define_candidates(grid = list(
  truncations = c(0.01, 0.05),
  libraries   = list(glm = "SL.glm")))
length(cands)
one <- define_candidates("glm_t01", g_library = "SL.glm",
                         truncation = 0.01)
}
