% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{fit_ps_glm}
\alias{fit_ps_glm}
\title{Fit a Logistic Regression Propensity Score Model}
\usage{
fit_ps_glm(lock, truncate = 0.01)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link[=create_analysis_lock]{create_analysis_lock()}}.}

\item{truncate}{Numeric in (0, 0.5); propensity scores are bounded to
\verb{[truncate, 1 - truncate]} to avoid extreme weights. Default: \code{0.01}.}
}
\value{
An object of class \code{ps_fit} (same structure as
\code{\link[=fit_ps_superlearner]{fit_ps_superlearner()}}, but with \code{method = "glm"} and \code{glm_fit}
instead of \code{sl_fit}).
}
\description{
A conventional logistic-regression alternative to \code{\link[=fit_ps_superlearner]{fit_ps_superlearner()}}
for use when \code{SuperLearner} is not installed or for rapid sensitivity
analyses.
}
\examples{
dat  <- sim_func1(n = 200, seed = 1)
lock <- create_analysis_lock(
  data = dat, treatment = "treatment", outcome = "event_24",
  covariates = c("age", "sex", "biomarker"), seed = 1L
)
ps_fit <- fit_ps_glm(lock)
print(ps_fit)

}
