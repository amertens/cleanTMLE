% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_tools.R
\name{nc_ladder_verdict}
\alias{nc_ladder_verdict}
\title{Check Point 3 Verdict from Locked Negative-Control Criteria}
\usage{
nc_ladder_verdict(nc_ladder, criteria)
}
\arguments{
\item{nc_ladder}{An \code{nc_ladder} object from
\code{\link[=run_negative_control_ladder]{run_negative_control_ladder()}}, or its table.}

\item{criteria}{The locked \code{nc_criteria} list (see
\code{\link[=create_analysis_lock]{create_analysis_lock()}}).}
}
\value{
A list with \code{by_domain} (one row per rung per domain),
\code{by_rung} (one row per rung with the verdict), and the echoed
\code{criteria}.
}
\description{
Grades a negative-control ladder against the criteria declared on the
lock before outcome access, and against nothing else: the null band,
the in-band rule (point estimate or whole confidence interval), the
minimum number of estimable controls per domain, and the consistency
rule within a domain. Per rung and domain the reading is \code{pass},
\code{fail}, or \code{insufficient} (fewer estimable controls than
\code{min_per_domain}); the rung verdict is STOP when any domain fails,
FLAG when any domain is insufficient, and GO otherwise.
}
