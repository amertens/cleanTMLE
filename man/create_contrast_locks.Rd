% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_tools.R
\name{create_contrast_locks}
\alias{create_contrast_locks}
\title{One Lock per Contrast from a Multi-Level Treatment}
\usage{
create_contrast_locks(
  data,
  treatment_factor,
  contrasts,
  outcome,
  covariates,
  sl_library = c("SL.glm", "SL.mean"),
  seed = 42L,
  negative_controls = NULL,
  treatment_name = ".A_contrast",
  cleanroom_enabled = TRUE
)
}
\arguments{
\item{data}{A data.frame.}

\item{treatment_factor}{Name of the multi-level treatment column.}

\item{contrasts}{A named list; each element is
\verb{list(treated = <levels>, control = <levels>, label = <text>)}.}

\item{outcome, covariates, sl_library, seed}{As in
\code{\link[=create_analysis_lock]{create_analysis_lock()}}.}

\item{negative_controls}{Optional character vector of negative-control
columns registered on every lock via \code{\link[=define_negative_control]{define_negative_control()}}.
Registration happens here, before any downstream variance filtering,
and a registered control is never dropped silently.}

\item{treatment_name}{Name of the derived binary column. Default
\code{".A_contrast"}.}

\item{cleanroom_enabled}{As in \code{\link[=create_analysis_lock]{create_analysis_lock()}}. Default TRUE.}
}
\value{
A named list of class \code{contrast_locks}.
}
\description{
Builds a named list of \code{cleanroom_lock} objects, one per requested
contrast, from a factor (or character) treatment with shared covariate
construction, shared negative controls, and a shared seed. Each lock's
data are the rows belonging to that contrast's arms, with a binary
treatment column added; each then gets its own propensity fit, support
assessment, gate and estimand ladder downstream.
}
\examples{
\dontrun{
locks <- create_contrast_locks(
  dat, "transport3",
  contrasts = list(
    C1 = list(treated = "Rescue.Co ambulance",
              control = c("Non-Rescue.Co ambulance", "Non-ambulance"),
              label = "Rescue.Co vs everyone else")),
  outcome = "death_by_6mo", covariates = covs)
}
}
