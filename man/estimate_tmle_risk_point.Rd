% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmle.R
\name{estimate_tmle_risk_point}
\alias{estimate_tmle_risk_point}
\title{Point-Treatment TMLE for Binary Outcomes}
\usage{
estimate_tmle_risk_point(
  data,
  treatment = NULL,
  outcome = NULL,
  covariates = NULL,
  family = "binomial",
  sl_library = c("SL.glm", "SL.mean"),
  truncate = 0.01,
  spec = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame.}

\item{treatment}{Character; name of the binary treatment variable.}

\item{outcome}{Character; name of the binary outcome variable.}

\item{covariates}{Character vector of covariate column names (W).}

\item{family}{\code{"binomial"} (default) for binary outcomes.}

\item{sl_library}{Character vector of SuperLearner algorithms.
Default: \code{c("SL.glm", "SL.mean")}. Use \code{"SL.glmnet"}, \code{"SL.ranger"},
etc. if those packages are installed.}

\item{truncate}{Truncation level for propensity scores (default 0.01,
meaning PS is bounded to the interval 0.01 to 0.99).}

\item{spec}{Optional \code{cr_spec} object. If provided, extracts
treatment/outcome/covariate info from it.}

\item{...}{Additional arguments passed to \code{tmle::tmle()}.}
}
\value{
An object of class \code{tmle_fit} (inherits from \code{cr_result})
containing:
\itemize{
\item \code{estimates} - list with ATE (RD) and RR estimates, SEs, CIs
\item \code{tmle_obj} - the raw tmle object from the \pkg{tmle} package
\item \code{influence_curve} - the efficient influence curve values
}
}
\description{
Estimates the average treatment effect (ATE) and optionally the risk
ratio (RR) for a binary outcome using the \pkg{tmle} package with
\pkg{SuperLearner} for nuisance parameter estimation.
}
\details{
This function requires the \pkg{tmle} and \pkg{SuperLearner} packages.
It uses the TMLE framework to estimate the causal risk difference
(ATE = E[Y(1)] - E[Y(0)]) with influence-curve based standard errors
and confidence intervals.
\subsection{SuperLearner}{

The nuisance parameters (outcome regression Q and propensity score g)
are estimated using SuperLearner ensemble learning. The default library
uses GLM and the marginal mean. For better performance, add more
learners like \code{"SL.glmnet"}, \code{"SL.ranger"}, \code{"SL.xgboost"}.
}
}
\examples{
\dontrun{
dat <- sim_func1(n = 500)
dat$event_24 <- as.integer(dat$event == 1 & dat$time <= 24)
fit <- estimate_tmle_risk_point(
  data = dat,
  treatment = "treatment",
  outcome = "event_24",
  covariates = c("age", "sex", "biomarker"),
  sl_library = c("SL.glm", "SL.mean")
)
print(fit)
}

}
