% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{create_simple_lock}
\alias{create_simple_lock}
\title{Create a Simple (Non-Clean-Room) Analysis Lock}
\usage{
create_simple_lock(
  data,
  treatment,
  outcome,
  covariates,
  sl_library = c("SL.glm", "SL.mean"),
  plasmode_reps = 100L,
  seed = 42L
)
}
\arguments{
\item{data}{A data.frame containing covariates, treatment, and outcome.}

\item{treatment}{Character; name of the binary treatment column.}

\item{outcome}{Character; name of the outcome column.}

\item{covariates}{Character vector of baseline covariate column names.}

\item{sl_library}{Character vector of SuperLearner algorithm names.
Default: \code{c("SL.glm", "SL.mean")}.}

\item{plasmode_reps}{Integer; number of plasmode replicates for Stage 2b
feasibility evaluation. Default: 100.}

\item{seed}{Integer; random seed for reproducibility. Default: 42.}
}
\value{
A \code{cleanroom_lock} object with \code{cleanroom_enabled = FALSE}.
}
\description{
A convenience wrapper around \code{\link[=create_analysis_lock]{create_analysis_lock()}} that disables the
software-enforced clean-room machinery (outcome guard, gate authorisation,
audit-trail requirement). The resulting lock can be passed to the
estimation functions (\code{\link[=run_clean_tmle]{run_clean_tmle()}}, \code{\link[=run_ipcw_tmle]{run_ipcw_tmle()}},
\code{\link[=run_matched_tmle]{run_matched_tmle()}}, \code{\link[=estimate_ipwrisk]{estimate_ipwrisk()}}, \code{\link[=estimate_gcomprisk]{estimate_gcomprisk()}},
\code{\link[=estimate_aipwrisk]{estimate_aipwrisk()}}, etc.) without first calling
\code{\link[=authorize_outcome_analysis]{authorize_outcome_analysis()}}. The lock fingerprint and the design
diagnostics (\code{compute_ps_diagnostics()}, \code{clean_weight_diagnostics()},
\code{love_plot()}) remain available.
}
\details{
Use this constructor when you want cleanTMLE as a plain outcome-blind
RWE pipeline (TMLE, IPTW, g-computation, AIPW, IPCW-TMLE) without the
staged-analysis governance layer. Outcome blindness is then the
analyst's responsibility, not software-enforced.

For high-stakes confirmatory studies, use \code{\link[=create_analysis_lock]{create_analysis_lock()}}
(the default) and the full staged workflow.
}
\examples{
dat  <- sim_func1(n = 200, seed = 1)
lock <- create_simple_lock(
  data       = dat,
  treatment  = "treatment",
  outcome    = "event_24",
  covariates = c("age", "sex", "biomarker"),
  seed       = 1
)
isFALSE(lock$cleanroom_enabled)

}
