% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{attach_estimand}
\alias{attach_estimand}
\title{Attach an Estimand to an Analysis Lock}
\usage{
attach_estimand(
  lock,
  description = NULL,
  population = NULL,
  treatment_strategies = NULL,
  outcome_label = NULL,
  followup = NULL,
  contrast = "risk_difference",
  statistical_estimand = NULL
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link{create_analysis_lock}}.}

\item{description}{Character; free-text description of the causal question.}

\item{population}{Character; description of the target population.}

\item{treatment_strategies}{Character vector of length 2 describing
the treatment contrast (e.g. \code{c("Drug A", "Placebo")}).}

\item{outcome_label}{Character; description of the primary outcome.}

\item{followup}{Character; follow-up horizon description (e.g. "24 months").}

\item{contrast}{Character; estimand type, e.g. \code{"risk_difference"},
\code{"risk_ratio"}, \code{"hazard_ratio"}.}

\item{statistical_estimand}{Character; description of the statistical
(observed-data) estimand under identification assumptions.}
}
\value{
A modified \code{cleanroom_lock} with element \code{estimand}.
The lock hash is unchanged (estimand metadata does not invalidate the
analytic specification).
}
\description{
Enriches a \code{cleanroom_lock} with a structured description of the
causal and statistical estimand.
}
\section{Clean-room stage}{
 Stage 1a (pre-outcome).
}

\examples{
dat  <- sim_func1(n = 200, seed = 1)
lock <- create_analysis_lock(dat, "treatment", "event_24",
                             c("age", "sex", "biomarker"), seed = 1)
lock <- attach_estimand(lock,
  description           = "Effect of treatment on 24-month event risk",
  population            = "Adults with simulated disease",
  treatment_strategies  = c("Treatment", "Control"),
  outcome_label         = "Primary event by 24 months",
  followup              = "24 months",
  contrast              = "risk_difference",
  statistical_estimand  = "E[Y(1)] - E[Y(0)] under SUTVA + positivity"
)
print(lock)

}
