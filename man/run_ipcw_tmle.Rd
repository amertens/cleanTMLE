% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{run_ipcw_tmle}
\alias{run_ipcw_tmle}
\title{Run IPCW-Weighted TMLE for a Binary Outcome with Missing-At-Random Y}
\usage{
run_ipcw_tmle(
  lock,
  ps_fit = NULL,
  censoring_library = NULL,
  weight_truncation = 0.99,
  allow_outcome_access = FALSE,
  override_clean_room = NULL
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link[=create_analysis_lock]{create_analysis_lock()}} with the
outcome unmasked.}

\item{ps_fit}{Optional \code{ps_fit} from \code{\link[=fit_ps_superlearner]{fit_ps_superlearner()}}. If
\code{NULL} (default), the censoring model still uses the locked
SuperLearner library; the treatment mechanism is re-fit by
\code{tmle::tmle()}.}

\item{censoring_library}{SuperLearner library used for the censoring
model \code{g(Delta = 1 | A, W)}. Default \code{NULL} uses the lock's
SuperLearner library.}

\item{weight_truncation}{Upper-quantile cap for the IPCW weights to
bound extreme values. Default \code{0.99}.}

\item{allow_outcome_access}{Logical; if \code{TRUE}, skips the outcome-access check. Default \code{FALSE}.}

\item{override_clean_room}{Deprecated. Use \code{allow_outcome_access}.}
}
\value{
A list of class \code{tmle_fit} (and \code{cr_result}) containing the
ATE estimate, IPCW summary, and the underlying \code{tmle::tmle()} fit.
The returned object is compatible with \code{\link[=summarize_cleanroom_results]{summarize_cleanroom_results()}}
and \code{\link[=forest_plot]{forest_plot()}}.
}
\description{
Targets the marginal risk difference on the full cohort when the
outcome \code{Y} has missing values, using inverse-probability-of-
censoring weighting to correct for missing-at-random follow-up.
Internally, a response model
\eqn{P(\text{observed} \mid A, W)} is fit by SuperLearner (or GLM if
SuperLearner is unavailable), stabilised inverse-probability weights
are constructed, and the cohort is passed to \code{tmle::tmle()} with the
\code{Delta} argument so that the package's targeting step uses the
censoring weights internally rather than dropping incomplete rows.
}
\section{Clean-room stage}{
 Stage 4 (accesses the outcome).
}

\examples{
\dontrun{
lock_unmasked <- unmask_outcome(lock, lock_pre_mask)
ps_fit        <- fit_ps_superlearner(lock_unmasked)
ipcw_fit      <- run_ipcw_tmle(lock_unmasked, ps_fit)
print(ipcw_fit)
}

}
