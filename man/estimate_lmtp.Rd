% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmle.R
\name{estimate_lmtp}
\alias{estimate_lmtp}
\title{Longitudinal TMLE via \pkg{lmtp}}
\usage{
estimate_lmtp(
  data,
  treatment_vars,
  outcome,
  baseline = NULL,
  time_varying = NULL,
  censor_vars = NULL,
  shift_fun = NULL,
  outcome_type = c("binomial", "continuous"),
  sl_library = c("SL.glm", "SL.mean"),
  id = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame in long or wide format.}

\item{treatment_vars}{Character vector of treatment variable names at
each time point (e.g., \code{c("A_1", "A_2", "A_3")}).}

\item{outcome}{Character; name of the outcome variable.}

\item{baseline}{Character vector of baseline covariate names.}

\item{time_varying}{Optional list of time-varying covariate names,
one vector per time point.}

\item{censor_vars}{Optional character vector of censoring indicator
names at each time point.}

\item{shift_fun}{A function defining the intervention. Default is a
static intervention setting treatment to 1. The function should take
\verb{(data, trt)} and return modified treatment values.}

\item{outcome_type}{\code{"binomial"} or \code{"continuous"}.}

\item{sl_library}{SuperLearner library.}

\item{id}{Optional character; name of the subject ID column.}

\item{...}{Additional arguments passed to \code{lmtp::lmtp_tmle()}.}
}
\value{
An object of class \code{tmle_fit}.
}
\description{
Estimates the mean potential outcome under static, dynamic, or
stochastic treatment interventions using the \pkg{lmtp} package.
}
\details{
This function requires the \pkg{lmtp} package. It implements the
longitudinal modified treatment policy (LMTP) framework for
causal inference with time-varying treatments.
}
\examples{
\dontrun{
# Simple 2-time-point example
dat <- data.frame(
  id = rep(1:200, each = 2),
  time = rep(1:2, 200),
  A = rbinom(400, 1, 0.5),
  L = rnorm(400),
  Y = rbinom(400, 1, 0.3)
)
# Static intervention: set all A = 1
fit <- estimate_lmtp(
  data = dat,
  treatment_vars = c("A"),
  outcome = "Y",
  baseline = c("L"),
  shift_fun = function(data, trt) rep(1, length(data[[trt]]))
)
}

}
