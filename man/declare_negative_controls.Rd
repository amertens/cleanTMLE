% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{declare_negative_controls}
\alias{declare_negative_controls}
\title{Declare Negative Controls on the Lock}
\usage{
declare_negative_controls(
  lock,
  variables,
  types = "outcome",
  descriptions = NULL,
  domains = NULL,
  nc_criteria = NULL
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock}.}

\item{variables}{Character vector of negative-control column names.}

\item{types}{Character vector (recycled): \code{"outcome"} or
\code{"exposure"}. Default \code{"outcome"}.}

\item{descriptions}{Optional character vector (recycled) of
free-text descriptions.}

\item{domains}{Optional character vector (recycled) of Muntner et
al. (2024) domains: \code{"confounding_by_indication"},
\code{"functional_status"}, \code{"health_seeking_behavior"},
\code{"access_to_healthcare"}, or \code{"other"}.}

\item{nc_criteria}{Optional prespecified decision criteria (see
\code{\link[=create_analysis_lock]{create_analysis_lock()}}). Accepted only when none were declared at
lock creation; recorded in the design log as a post-creation
declaration.}
}
\value{
The lock with the controls registered, the declaration
logged, and (when given) \code{nc_criteria} set.
}
\description{
Registers one or more negative-control variables, with their types,
descriptions, and Muntner et al. (2024) confounding domains, and
writes the declaration into the design log. Controls declared through
\code{create_analysis_lock(negative_controls = )} can be enriched here
with domains; criteria declared at lock creation
(\verb{nc_criteria = }) are fingerprinted, while criteria declared here
afterwards are recorded in the design log with a timestamp (the
fingerprint cannot change after creation).
}
\examples{
dat  <- sim_func1(n = 200, seed = 1)
lock <- create_analysis_lock(dat, "treatment", "event_24",
                             c("age", "sex", "biomarker"), seed = 1)
lock <- declare_negative_controls(lock, "nc_outcome",
  domains = "health_seeking_behavior",
  nc_criteria = list(null_band = 0.02))
}
