% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_dq_heatmap}
\alias{plot_dq_heatmap}
\title{Heatmap of candidate performance across data-quality threats}
\usage{
plot_dq_heatmap(
  x,
  metric = c("rmse", "bias", "coverage", "emp_sd", "se_cal"),
  effect_size = NULL,
  scenarios = NULL,
  label_values = TRUE,
  digits = 3L,
  low = "#FFF5F0",
  high = "#67000D",
  ...
)
}
\arguments{
\item{x}{A \code{plasmode_dq_results} object from \code{\link[=run_plasmode_dq_stress]{run_plasmode_dq_stress()}}.}

\item{metric}{Character; metric to display. One of \code{"rmse"} (default),
\code{"bias"}, \code{"coverage"}, \code{"emp_sd"}, or \code{"se_cal"}.}

\item{effect_size}{Numeric scalar; which effect size to display. \code{NULL}
(default) uses the first available effect size.}

\item{scenarios}{Character vector of scenario names to include, or \code{NULL}
(default) for all scenarios in the metrics table.}

\item{label_values}{Logical; print the metric value inside each tile.
Default \code{TRUE}.}

\item{digits}{Integer; decimal places for tile labels. Default \code{3}.}

\item{low, high}{Character colour strings for the gradient endpoints.
Defaults (\code{"#FFF5F0"} to \code{"#67000D"}) map low values to near-white and
high values to dark red, which is appropriate for RMSE and bias (lower is
better). For coverage, where higher is better, pass
\verb{low = "#67000D", high = "#009E73"} to reverse the direction.}

\item{...}{Not currently used.}
}
\value{
A ggplot object.
}
\description{
Plots a tile grid showing RMSE, bias, coverage, or another operating
characteristic from a \code{\link[=run_plasmode_dq_stress]{run_plasmode_dq_stress()}} result. Candidates run
along the x-axis; scenario-by-severity combinations run along the y-axis,
with the undisturbed baseline row at the top so degradation is immediately
visible.
}
\examples{
\dontrun{
dq <- run_plasmode_dq_stress(lock, tmle_candidates, reps = 20)
plot_dq_heatmap(dq)
plot_dq_heatmap(dq, metric = "coverage")
plot_dq_heatmap(dq, metric = "bias", scenarios = c("none", "unmeasured_U"))
}

}
\seealso{
\code{\link[=run_plasmode_dq_stress]{run_plasmode_dq_stress()}}, \code{\link[=summarize_dq_degradation]{summarize_dq_degradation()}}
}
