% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plasmode_extensions.R
\name{build_sl_library}
\alias{build_sl_library}
\title{Build a SuperLearner Library by Role and Sample Size}
\usage{
build_sl_library(
  role = c("Q", "g", "Delta"),
  n_eff,
  p = NULL,
  preset = c("auto", "small_n", "default", "rich", "very_rich"),
  include_screeners = FALSE
)
}
\arguments{
\item{role}{One of \code{"Q"}, \code{"g"}, \code{"Delta"}.}

\item{n_eff}{Effective sample size (from \code{\link[=compute_n_eff]{compute_n_eff()}}).}

\item{p}{Number of candidate predictors.}

\item{preset}{One of \code{"auto"}, \code{"small_n"}, \code{"default"}, \code{"rich"},
\code{"very_rich"}.}

\item{include_screeners}{Logical; if TRUE wrap learners with
\code{c(learner, "screen.corP")} to pair them with a correlation
screener (Phillips p.1283).}
}
\value{
A list with \code{library} (character vector or list of
\code{c(learner, screener)} pairs), \code{cv_V} (recommended V), and
\code{stratify_cv} (logical, TRUE for \code{g} / \code{Delta}, FALSE for \code{Q}).
}
\description{
Returns a role-aware SuperLearner library keyed by effective
sample size and dimensionality. Implements the published
workshop-default specifications from Gruber et al. 2023 SAP
supplement (Checklists C-E) and Phillips et al. 2023 Steps 2-5.
}
\details{
Roles:
\itemize{
\item \code{"Q"}: initial outcome regression. Family matches the outcome
(binomial here).
\item \code{"g"}: propensity-score model. Family is binomial; CV is
stratified.
\item \code{"Delta"}: missingness / censoring response model. Same as \code{g}.
}

Presets (selected by \code{preset}; \code{"auto"} picks by \code{n_eff}):
\itemize{
\item \code{"small_n"}: GLM + GLM with main effects + ranger-shallow.
\item \code{"default"}: GLM + glmnet + GAM (Phillips workshop default).
\item \code{"rich"}: GLM + glmnet + GAM + ranger + xgboost.
\item \code{"very_rich"}: above + BART (via \code{tmle.SL.dbarts2}) + HAL
(if installed).
}
}
\examples{
build_sl_library(role = "g", n_eff = 300, p = 10, preset = "default")

}
\references{
Phillips RV, et al. (2023); Gruber S, et al. (2023)
SAP supplement Checklists C-E.
}
