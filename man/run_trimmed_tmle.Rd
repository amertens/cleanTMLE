% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand_estimators.R
\name{run_trimmed_tmle}
\alias{run_trimmed_tmle}
\title{Trimmed ATE with a Propensity Refit}
\usage{
run_trimmed_tmle(
  lock,
  ps_fit,
  levels = c(0.05, 0.1),
  rule = c("fixed", "crump"),
  family = "binomial",
  sl_library = NULL,
  gbound = NULL,
  cv_folds = 10L,
  prescreen_g = FALSE,
  use_ipcw = FALSE,
  thresholds = support_thresholds(),
  min_n = 100L,
  min_per_arm = 30L,
  seed = NULL,
  allow_outcome_access = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock}.}

\item{ps_fit}{A \code{ps_fit} on the full cohort; its untruncated scores define
the trim.}

\item{levels}{Trim levels tried in order (g in \verb{[l, 1 - l]}). Default
\code{c(0.05, 0.10)}.}

\item{rule}{\code{"fixed"} uses \code{levels}; \code{"crump"} derives the level from the
data (Crump et al. 2009) and prepends it.}

\item{family, sl_library, gbound, cv_folds, prescreen_g, seed}{As in
\code{\link[=run_att_tmle]{run_att_tmle()}}.}

\item{use_ipcw}{Estimate with the censoring mechanism (\code{Delta}) on the
trimmed subset. Default FALSE (complete case).}

\item{thresholds}{\code{\link[=support_thresholds]{support_thresholds()}} used to judge the refit.}

\item{min_n, min_per_arm}{Feasibility floor after trimming. Defaults 100
and 30.}

\item{allow_outcome_access}{Bypass the outcome guard. Default FALSE.}

\item{verbose}{Print the per-level path. Default TRUE.}
}
\value{
An object of class \code{trimmed_tmle_fit}: the estimate at the first
level that cleared the gate, dropped counts by arm, the refit's support
assessment, the population label, the implausibility flags, and the
record of levels tried.
}
\description{
Trims to the common-support band, refits the propensity model on the
trimmed subset (reusing the full-cohort fit would describe the overlap of
a population that no longer exists), reassesses support, and estimates
the ATE on the subset only when the refit clears the gate; otherwise the
next, tighter level is tried. Trimming changes the estimand: the result
is the effect among patients whose covariates could plausibly have
produced either arm, and every row is labelled with that population.
}
\references{
Crump RK, Hotz VJ, Imbens GW, Mitnik OA (2009) Biometrika
96:187-199.
}
\keyword{internal}
