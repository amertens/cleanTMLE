% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify.R
\name{identify}
\alias{identify_outcome}
\alias{identify_censoring}
\alias{identify_treatment}
\alias{identify_competing_risk}
\alias{identify_subject}
\alias{identify_interval}
\alias{identify_missing}
\title{Identify Model Components}
\usage{
identify_outcome(spec, name, formula = NULL,
  type = c("time_to_event", "binary", "continuous"), ...)

identify_censoring(spec, name, formula = NULL,
  model = c("coxph", "glm"), ...)

identify_treatment(spec, name, formula = NULL,
  model = c("glm", "multinom"), ...)

identify_competing_risk(spec, name, event_value)

identify_subject(spec, name)

identify_interval(spec, start, stop)

identify_missing(spec, ...)
}
\arguments{
\item{spec}{A \code{cr_spec} object from \code{specify_models()}.}
\item{name}{The column name (bare or quoted) identifying the variable.}
\item{formula}{A one-sided formula for the model (e.g., \code{~ age + sex}).}
\item{type}{For outcomes: one of \code{"time_to_event"}, \code{"binary"}, or \code{"continuous"}.}
\item{model}{For censoring: \code{"coxph"} or \code{"glm"}. For treatment: \code{"glm"} or \code{"multinom"}.}
\item{event_value}{For competing risks: the value in the event-type column indicating the event of interest.}
\item{start, stop}{Column names for interval start and stop times.}
\item{...}{Additional arguments passed to model fitting functions.}
}
\value{
The modified \code{cr_spec} object (for piping).
}
\description{
These functions add component specifications to a \code{cr_spec} object created
by \code{specify_models()}.
}
