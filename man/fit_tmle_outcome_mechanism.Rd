% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{fit_tmle_outcome_mechanism}
\alias{fit_tmle_outcome_mechanism}
\title{Fit TMLE Outcome Mechanism}
\usage{
fit_tmle_outcome_mechanism(
  lock,
  g_fit,
  sl_library = NULL,
  override_clean_room = FALSE
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link[=create_analysis_lock]{create_analysis_lock()}}.}

\item{g_fit}{A \code{tmle_mechanism} object with \code{type = "treatment"} from
\code{\link[=fit_tmle_treatment_mechanism]{fit_tmle_treatment_mechanism()}}.}

\item{sl_library}{Optional SuperLearner library override. If \code{NULL},
uses the locked primary TMLE spec Q-library if available, then falls
back to \code{lock$sl_library}.}

\item{override_clean_room}{Logical; if \code{TRUE}, skips the outcome-
access check.  Default \code{FALSE}.}
}
\value{
An object of class \code{tmle_mechanism} with \code{type = "outcome"}
containing initial outcome predictions \code{Q_a1}, \code{Q_a0}, and \code{Q_aw}.
}
\description{
Fits the outcome mechanism Q(A,W) = E[Y|A,W] using SuperLearner (or
logistic regression as a fallback). This step accesses the outcome and
\strong{must only be called in Stage 4} (after outcome unblinding).
}
\details{
If the lock contains a locked primary TMLE specification (from
\code{\link[=lock_primary_tmle_spec]{lock_primary_tmle_spec()}}), the Q-library from that specification is
used by default.
}
\section{Clean-room stage}{
 Stage 4 (accesses the real outcome).
}

