% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand_estimators.R
\name{run_att_tmle}
\alias{run_att_tmle}
\title{Complete-Case ATT via TMLE, Never Through the Censoring Mechanism}
\usage{
run_att_tmle(
  lock,
  family = "binomial",
  sl_library = NULL,
  gbound = NULL,
  cv_folds = 10L,
  prescreen_g = FALSE,
  reps = 1L,
  seed = NULL,
  allow_outcome_access = FALSE
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock}.}

\item{family}{\code{"binomial"} or \code{"gaussian"}.}

\item{sl_library}{SuperLearner library for Q and g; defaults to the locked
candidate's library, else the lock library.}

\item{gbound}{Numeric bound, named rule (see
\code{\link[=resolve_truncation_rule]{resolve_truncation_rule()}}), or \code{NULL} for the tmle package default.}

\item{cv_folds}{V for Q and g cross-validation. Default 10.}

\item{prescreen_g}{Passed to \code{tmle::tmle(prescreenW.g = )} when the
installed version supports it. Default FALSE; see the builder's note.}

\item{reps}{Seed-averaging replicates; distinct seeds are drawn from
\code{seed} and combined with the multiple-imputation rule
\verb{se^2 = mean(var) + (1 + 1/K) var(psi)}. Default 1.}

\item{seed}{Base seed. Defaults to \code{lock$seed}.}

\item{allow_outcome_access}{Bypass the outcome guard. Default FALSE.}
}
\value{
An object of class \code{att_fit} (inherits \code{cr_result}): the ATT with
SE, CI and p-value, the same-fit ATE, the named estimand, n, and the
implausibility flags.
}
\description{
Estimates the effect among the treated on the rows with an observed
outcome, named as its own estimand:
\verb{E[Y(1) - Y(0) | A = 1, outcome observed]}. The delegation to
\code{\link[tmle:tmle]{tmle::tmle()}} never passes \code{Delta}: under the censoring mechanism the
package ATT loses double robustness (with the outcome model misspecified
its bias is large and coverage collapses, while the ATE path is
unaffected), so the complete-case fit is consistent for its own estimand
with no missingness model at all. The ATE from the same fit is returned
beside the ATT; with good overlap the two agree, and their gap under poor
overlap is itself the diagnostic. The treatment-model specification is
built by the same internal builder as every other delegation in the
package, so the ATT's propensity model cannot silently differ from the
ATE's.
}
\keyword{internal}
