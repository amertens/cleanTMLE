% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{gate_check}
\alias{gate_check}
\title{Evaluate a Plasmode-Simulation Gate (GO / FLAG / STOP)}
\usage{
gate_check(metrics, scenario_name, targets, method = "TMLE")
}
\arguments{
\item{metrics}{A data.frame with at least columns \code{bias}, \code{coverage},
\code{emp_sd}, and \code{mean_se}, plus a \code{candidate} column (from the updated
plasmode API) or a \code{method} column (legacy API).}

\item{scenario_name}{Character label for the scenario (used in output).}

\item{targets}{A list with elements \code{max_abs_bias} (maximum tolerable
absolute bias), \code{min_coverage} (minimum acceptable 95 percent CI
coverage), \code{se_sd_low} (lower bound for SE / empirical-SD ratio), and
\code{se_sd_high} (upper bound for SE / empirical-SD ratio).}

\item{method}{Character; which row to evaluate.
Matched against the \code{candidate} column if it exists, otherwise the
\code{method} column. Default: \code{"TMLE"}.}
}
\value{
A list with elements \code{decision} (one of \code{"GO"}, \code{"FLAG"}, or
\code{"STOP"}), \code{table} (a one-row data.frame summarising the evaluation),
and \code{scenario} (the \code{scenario_name} used).
}
\description{
Compares performance metrics from a plasmode-simulation replicate set
against pre-specified targets and returns a structured GO / FLAG / STOP
decision. This implements the checkpoint logic described in the
clean-room staged workflow: if bias, coverage, and SE-calibration all
pass, the decision is \strong{GO}; if bias and coverage pass but
SE-calibration fails, the decision is \strong{FLAG} (proceed with caution);
otherwise the decision is \strong{STOP}.
}
\examples{
metrics <- data.frame(
  candidate = c("glm_t01", "glm_t05"),
  bias      = c(0.002, 0.005),
  coverage  = c(0.95,  0.93),
  emp_sd    = c(0.03,  0.04),
  mean_se   = c(0.031, 0.041),
  stringsAsFactors = FALSE
)
targets <- list(
  max_abs_bias = 0.01,
  min_coverage = 0.90,
  se_sd_low    = 0.8,
  se_sd_high   = 1.2
)
gate_check(metrics, "Example", targets, method = "glm_t01")

}
