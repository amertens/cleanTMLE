% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{checkpoint_cohort_adequacy}
\alias{checkpoint_cohort_adequacy}
\title{Check Point 1: Cohort Adequacy and Precision}
\usage{
checkpoint_cohort_adequacy(
  lock,
  min_n_per_arm = 50L,
  min_events = 20L,
  min_prevalence = 0.01,
  stop_n_per_arm = NULL,
  stop_min_events = NULL
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock}.}

\item{min_n_per_arm}{Integer; minimum acceptable sample size per
treatment arm. A shortfall below this (but above \code{stop_n_per_arm})
is a FLAG. Default: 50.}

\item{min_events}{Integer; minimum acceptable total outcome events.
Default: 20.}

\item{min_prevalence}{Numeric; minimum outcome prevalence. Default: 0.01.}

\item{stop_n_per_arm}{Integer; a per-arm count strictly below this
forces a STOP rather than a FLAG. Default \code{NULL} = 20 (the former
hard-coded floor).}

\item{stop_min_events}{Integer; a total event count strictly below this
forces a STOP. Default \code{NULL} = \code{min_events}.}
}
\value{
A \code{cleantmle_checkpoint} for Check Point 1.
}
\description{
Generates a structured report assessing whether the analytic cohort has
adequate sample size, event counts, and positivity for the planned
analysis.  Returns a \code{cleantmle_checkpoint} with a GO / FLAG /
STOP decision.
}
\section{Clean-room stage}{
 Stage 1b (pre-outcome for design-stage
summaries; event counts use the outcome variable).
}

\examples{
dat  <- sim_func1(n = 500, seed = 1)
lock <- create_analysis_lock(dat, "treatment", "event_24",
                             c("age", "sex", "biomarker"), seed = 1)
cp1 <- checkpoint_cohort_adequacy(lock)
print(cp1)

}
