% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance_methods.R
\name{.match_nn_pairs}
\alias{.match_nn_pairs}
\title{Greedy 1:1 nearest-neighbour matching on the logit propensity score}
\usage{
.match_nn_pairs(logit_ps, A, caliper = NULL)
}
\arguments{
\item{logit_ps}{Numeric vector of logit-propensity scores (all units).}

\item{A}{Integer/numeric treatment indicator (1 = treated, 0 = control).}

\item{caliper}{Maximum allowable distance on the logit-PS scale. Default
is \code{0.2 * sd(logit_ps)} (Rosenbaum and Rubin 1985 rule of thumb).}
}
\value{
A list with elements \code{match_idx} (integer row indices of the
matched dataset, treated rows followed by their control partners),
\code{treated_valid}, \code{control_valid}, and \code{n_matched}.
}
\description{
Pure-R implementation that mirrors \code{run_match_workflow()} exactly:
treated units are matched in order; each control unit may be used at
most once; matches that exceed \code{caliper} are dropped.
}
\keyword{internal}
