% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{fit_tmle_treatment_mechanism}
\alias{fit_tmle_treatment_mechanism}
\title{Fit TMLE Treatment Mechanism}
\usage{
fit_tmle_treatment_mechanism(lock, ps_fit = NULL, truncation = NULL)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link[=create_analysis_lock]{create_analysis_lock()}}.}

\item{ps_fit}{Optional \code{ps_fit} from \code{\link[=fit_ps_superlearner]{fit_ps_superlearner()}}. If provided,
the already-estimated propensity scores are reused.}

\item{truncation}{Numeric; PS truncation threshold.  If \code{NULL}
(default), uses the locked primary TMLE spec truncation if available,
otherwise 0.01.}
}
\value{
An object of class \code{tmle_mechanism} with \code{type = "treatment"}.
}
\description{
Fits the treatment mechanism g(A|W) = P(A=1|W) for use in the modular
TMLE workflow. Uses propensity scores from an existing \code{ps_fit} if
provided; otherwise re-estimates via SuperLearner. The outcome is never
accessed, making this step safe at any stage.
}
\details{
If the lock contains a locked primary TMLE specification (from
\code{\link[=lock_primary_tmle_spec]{lock_primary_tmle_spec()}}), the PS truncation threshold from that
specification is applied automatically.
}
\section{Clean-room stage}{
 Stage 2 / 4 (pre-outcome; does not access
the outcome).
}

