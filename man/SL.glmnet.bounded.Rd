% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sl_glmnet_bounded.R
\name{SL.glmnet.bounded}
\alias{SL.glmnet.bounded}
\title{Bounded glmnet SuperLearner Learner}
\usage{
SL.glmnet.bounded(
  Y,
  X,
  newX,
  family,
  obsWeights = rep(1, length(Y)),
  id = NULL,
  alpha = 1,
  nlambda = 20L,
  nfolds = 3L,
  maxit = 1000L,
  ...
)
}
\arguments{
\item{Y}{Outcome vector.}

\item{X}{Predictor data.frame.}

\item{newX}{Predictor data.frame for prediction.}

\item{family}{A \code{family} object (binomial or gaussian).}

\item{obsWeights}{Observation weights.}

\item{id}{Optional cluster ids (unused; present for SuperLearner API).}

\item{alpha}{Elastic-net mixing parameter. Default 1 (lasso).}

\item{nlambda}{Length of the lambda path. Default 20 (SuperLearner uses 100).}

\item{nfolds}{Cross-validation folds. Default 3 (SuperLearner uses 10).}

\item{maxit}{Maximum coordinate-descent iterations. Default 1000.}

\item{...}{Passed to \code{glmnet::cv.glmnet}.}
}
\value{
A list with \code{pred} (predictions for \code{newX}) and \code{fit}
(an object of class \code{SL.glmnet.bounded}).
}
\description{
A SuperLearner-compatible wrapper around \code{glmnet::cv.glmnet} with the
lambda path, cross-validation folds, and coordinate-descent iterations
bounded so the fit cannot run away on near-separable (near-positivity)
designs. Use it in place of \code{"SL.glmnet"} in a candidate's
\code{g_library} or \code{q_library} when the stress test generates
near-positivity designs.
}
