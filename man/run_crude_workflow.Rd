% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{run_crude_workflow}
\alias{run_crude_workflow}
\title{Run Crude (Unadjusted) Risk Difference Workflow}
\usage{
run_crude_workflow(lock, override_clean_room = FALSE)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link[=create_analysis_lock]{create_analysis_lock()}}.}

\item{override_clean_room}{Logical; if \code{TRUE}, skips the outcome-access check.  Default \code{FALSE}.}
}
\value{
A list with elements \code{estimate}, \code{se}, \code{ci_lower}, \code{ci_upper},
\code{p_value}, \code{r1} (risk in treated), and \code{r0} (risk in control).
}
\description{
Computes the unadjusted risk difference between treatment groups
without any propensity-score or covariate adjustment. Intended as
a benchmark comparator for the adjusted workflows.
}
\examples{
dat  <- sim_func1(n = 500, seed = 1)
lock <- create_analysis_lock(
  data = dat, treatment = "treatment", outcome = "event_24",
  covariates = c("age", "sex", "biomarker"), seed = 1
)
run_crude_workflow(lock)

}
