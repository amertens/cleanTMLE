% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_process_helpers.R
\name{checkpoint_weights}
\alias{checkpoint_weights}
\title{Pre-Outcome Weight Checkpoint}
\usage{
checkpoint_weights(
  weights,
  treatment = NULL,
  weight_type = "treatment",
  max_weight_threshold = 10,
  ess_floor = NULL,
  extreme_prop_threshold = 0.01,
  lock_hash = NA_character_
)
}
\arguments{
\item{weights}{Numeric vector of inverse-probability weights.}

\item{treatment}{Optional treatment indicator (0/1 or factor) of the
same length as \code{weights}.}

\item{weight_type}{Character label for the weight family (e.g.
\code{"treatment"}, \code{"censoring"}, \code{"missingness"},
\code{"treatment x censoring"}). Default \code{"treatment"}.}

\item{max_weight_threshold, ess_floor}{See \code{\link[=clean_weight_diagnostics]{clean_weight_diagnostics()}}.}

\item{extreme_prop_threshold}{Maximum acceptable proportion of
observations with weight greater than \code{max_weight_threshold}.
Default \code{0.01}.}

\item{lock_hash}{Optional character; lock fingerprint to attach to
the checkpoint for traceability.}
}
\value{
A \code{cleantmle_checkpoint} suitable for
\code{\link[=gate_all]{gate_all()}} / \code{\link[=record_checkpoint]{record_checkpoint()}}.
}
\description{
Wraps \code{\link[=clean_weight_diagnostics]{clean_weight_diagnostics()}} in a structured checkpoint object
compatible with \code{\link[=gate_all]{gate_all()}} and \code{\link[=authorize_outcome_analysis]{authorize_outcome_analysis()}}. Useful
when the weights themselves (treatment, censoring, or their product)
should be a formal pre-outcome diagnostic, not just a side note.
}
\details{
Decision logic:
\itemize{
\item \code{STOP} when ESS falls below \code{ess_floor} or the
maximum weight exceeds \code{max_weight_threshold} \emph{and} the
proportion of extreme weights exceeds \code{extreme_prop_threshold};
\item \code{FLAG} when either ESS or extreme-weight criteria fail
individually but not jointly;
\item \code{GO} otherwise.
}
Thresholds are user-supplied and should be prespecified.
}
\section{Clean-room stage}{
 Stage 2 (treatment weights) or Stage 4
pre-flight (censoring or missingness weights).
}

\examples{
set.seed(2)
n <- 400
A <- rbinom(n, 1, 0.4)
ps <- plogis(0.2 * rnorm(n))
w  <- ifelse(A == 1, 1 / ps, 1 / (1 - ps))
cp <- checkpoint_weights(w, treatment = A,
                         max_weight_threshold = 10,
                         ess_floor = 0.4 * n)
print(cp)

}
