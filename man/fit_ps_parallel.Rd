% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{fit_ps_parallel}
\alias{fit_ps_parallel}
\title{Fit Propensity Score with Parallel SuperLearner}
\usage{
fit_ps_parallel(lock, cluster = NULL, truncate = NULL, ...)
}
\arguments{
\item{lock}{A \code{cleanroom_lock} from \code{\link[=create_analysis_lock]{create_analysis_lock()}}.}

\item{cluster}{A \code{parallel} cluster object (e.g., from
\code{parallel::makeCluster()}).  If \code{NULL}, falls back to
sequential \code{SuperLearner::SuperLearner()}.}

\item{truncate}{Numeric in (0, 0.5) or \code{NULL}; PS truncation.}

\item{...}{Additional arguments passed to \code{SuperLearner::SuperLearner()}.}
}
\value{
A \code{ps_fit} object.
}
\description{
Like \code{\link{fit_ps_superlearner}} but accepts a PSOCK or FORK
cluster for parallel computation via
\code{SuperLearner::snowSuperLearner()}.  Essential for reasonable
runtime on Windows with non-trivial learner libraries.
}
\examples{
\dontrun{
cl <- parallel::makeCluster(2, type = "PSOCK")
ps_fit <- fit_ps_parallel(lock, cluster = cl)
parallel::stopCluster(cl)
}

}
