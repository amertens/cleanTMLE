% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand_ladder.R
\name{run_estimand_ladder}
\alias{run_estimand_ladder}
\title{Estimate the Declared Estimand Ladder}
\usage{
run_estimand_ladder(
  lock,
  ps_fit,
  ladder = NULL,
  support = NULL,
  feasibility = NULL,
  family = "binomial",
  use_ipcw = NULL,
  sl_library = NULL,
  gbound = NULL,
  cv_folds = 10L,
  prescreen_g = FALSE,
  seed = NULL,
  trim_levels = c(0.05, 0.1),
  override_reason = NULL,
  allow_outcome_access = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock}, normally after
\code{\link[=declare_estimand_ladder]{declare_estimand_ladder()}}.}

\item{ps_fit}{A \code{ps_fit} on the lock.}

\item{ladder}{The declared ladder; defaults to \code{lock$estimand_ladder}.}

\item{support}{A \code{\link[=assess_support]{assess_support()}} result; computed if \code{NULL}.}

\item{feasibility}{An \code{\link[=estimand_feasibility]{estimand_feasibility()}} result; computed if \code{NULL}.}

\item{family}{\code{"binomial"} or \code{"gaussian"}.}

\item{use_ipcw}{Estimate the ATE with the censoring mechanism (\code{Delta})
when the outcome has missing values. Default: TRUE when missing
outcomes are present.}

\item{sl_library, gbound, cv_folds, prescreen_g, seed}{As in
\code{\link[=run_att_tmle]{run_att_tmle()}}.}

\item{trim_levels}{Passed to \code{\link[=run_trimmed_tmle]{run_trimmed_tmle()}}.}

\item{override_reason}{Reason for estimating an infeasible primary;
recorded. Default \code{NULL} refuses.}

\item{allow_outcome_access}{Bypass the outcome guard. Default FALSE.}

\item{verbose}{Print the path. Default TRUE.}
}
\value{
An object of class \code{estimand_ladder_result}: \code{table} (one row per
estimated estimand), \code{fits} (the underlying objects), \code{support},
\code{feasibility}, \code{ladder}, and \code{design_log}.
}
\description{
The Stage 4 wrapper. Reads the support verdict and the feasibility table,
estimates the primary estimand when it is feasible under the declared
trigger plus every feasible fallback, and labels every returned row with
its estimand, its verdict, the caveat, and the implausibility flags. An
infeasible primary is refused unless \code{override_reason} is given, in which
case it is estimated anyway, labelled, and the override is recorded in the
returned object's design log.
}
