% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{stress_test}
\alias{stress_test}
\title{Run the Outcome-Blind Stress Test}
\usage{
stress_test(
  lock,
  candidates = NULL,
  threats = NULL,
  effect_sizes = c(0.05),
  reps = 50L,
  design = c("generate_treatment", "sample_treatment"),
  pilot_q0 = NULL,
  q0_library = NULL,
  max_fit_seconds = Inf,
  parallel = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock}.}

\item{candidates}{A \code{ct_candidates} set from \code{\link[=define_candidates]{define_candidates()}}
(or a plain list of candidate specifications).}

\item{threats}{\code{NULL} (baseline only), a preset name
(\code{"regulatory_standard"}, \code{"exploratory"}, \code{"stress"}), or a named
scenario list (see the engine documentation,
\code{?run_plasmode_dq_stress}).}

\item{effect_sizes}{Numeric vector of simulated risk differences.}

\item{reps}{Replicates per scenario cell.}

\item{design}{\code{"generate_treatment"} (default) or the deprecated
\code{"sample_treatment"}.}

\item{pilot_q0}{Baseline probabilities for \code{dgp_mode = "external_pilot"} locks.}

\item{q0_library}{Optional SuperLearner library for the plasmode
baseline outcome surface (hybrid mode); default is a
covariate-only logistic GLM.}

\item{max_fit_seconds}{Wall-clock bound per replicate's candidate
fits; a timed-out replicate is recorded as failed rather than
hanging the study. Default \code{Inf} (in-process fits).}

\item{parallel}{Logical; when \code{TRUE} and \pkg{furrr} is installed,
scenario cells run under the caller's \code{future::plan()}.}

\item{verbose}{Print progress.}
}
\value{
A \code{ct_stress} result (the stress-test metrics object; see
the engine documentation for fields).
}
\description{
The one plasmode loop: generates simulated outcomes under the lock's
locked generator mode (see \code{\link[=create_analysis_lock]{create_analysis_lock()}}), fits every
candidate on every replicate, and reports bias, RMSE, coverage, and
SE calibration per candidate. With \code{threats = NULL} this is the
clean-data baseline used for candidate selection; with a threat list
or preset name it adds the data-quality sweep, and when the lock
declares \code{dq_thresholds} the result carries the locked verdict and
tipping points (\code{\link[=dq_locked_verdict]{dq_locked_verdict()}}, \code{\link[=dq_tipping_points]{dq_tipping_points()}}).
\code{summary()} on the result is the degradation table.
}
\examples{
\dontrun{
cands <- define_candidates(grid = list(truncations = c(0.01, 0.05),
                                       libraries = list(glm = "SL.glm")))
st <- stress_test(lock, cands, threats = "exploratory", reps = 20)
summary(st)
}
}
