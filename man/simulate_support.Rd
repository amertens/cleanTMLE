% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_support.R
\name{simulate_support}
\alias{simulate_support}
\title{Outcome-Blind Support Simulation and Support Map}
\usage{
simulate_support(
  lock,
  ps_fit = NULL,
  surface = NULL,
  reps = 200L,
  design = c("generate_treatment", "sample_treatment", "parametric_bootstrap"),
  band = c(0.05, 0.95),
  q0_source = c("simulated", "negative_control", "auxiliary", "heldout",
    "primary_outcome"),
  q0_variable = NULL,
  heldout_fraction = 0.3,
  allow_outcome_q0 = FALSE,
  bias_tolerance = NULL,
  coverage_floor = 0.9,
  caliper_sd = 0.2,
  truncation = 0.01,
  checkpoint_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{lock}{A \code{cleanroom_lock}.}

\item{ps_fit}{Optional \code{ps_fit}; when supplied its propensity scores are
the generating propensity, otherwise a logistic model is fitted on the
lock's covariates.}

\item{surface}{A \code{\link[=support_surfaces]{support_surfaces()}} specification.}

\item{reps}{Integer; replicates per grid point. Default 200. Runs below
50 are tagged demonstration-only and the printed verdict is FLAG.}

\item{design}{\code{"generate_treatment"} (default) or the deprecated
\code{"sample_treatment"} (Shaw et al. 2025 artifact; warns).}

\item{band}{Numeric length 2; the trimming band for the trimmed-ATE
estimand and the region used by the support verdicts. Default
\code{c(0.05, 0.95)}.}

\item{q0_source}{Where the baseline outcome surface comes from:
\code{"simulated"} (default; the \code{\link[=support_surfaces]{support_surfaces()}} family anchored at
\code{base_rate}), \code{"negative_control"} or \code{"auxiliary"} (a covariate-only
GLM fitted to \code{q0_variable}, with the confounding axis adding outcome
dependence on the propensity direction on top of it), \code{"heldout"} (fit
on a design split whose rows are recorded in \code{heldout_rows} and must be
excluded from Stage 4), or \code{"primary_outcome"} (requires
\code{allow_outcome_q0 = TRUE}; not outcome-blind).}

\item{q0_variable}{Column name for \code{"negative_control"} / \code{"auxiliary"}.}

\item{heldout_fraction}{Fraction for \code{"heldout"}. Default 0.3.}

\item{allow_outcome_q0}{Logical; must be \code{TRUE} to fit Q0 on the primary
outcome. Default \code{FALSE}.}

\item{bias_tolerance}{Absolute bias a cell may show and still pass.
Default \code{0.5 * surface$effect}.}

\item{coverage_floor}{Minimum coverage for a cell to pass. Default 0.90.}

\item{caliper_sd}{Caliper for the matched-ATT estimator, in SDs of the
logit propensity. Default 0.2.}

\item{truncation}{Truncation for the lean estimators' fitted g.
Default 0.01.}

\item{checkpoint_file}{Optional path; results are saved after every grid
point and a partial run resumes from it.}

\item{verbose}{Print progress. Default TRUE.}
}
\value{
An object of class \code{support_simulation}: \code{metrics} (one row per
grid point per estimand), \code{map} (the pass/fail matrix), \code{feasible}
(per-estimand verdict over the whole grid), \code{surface}, \code{design}, \code{q0}
(source, variable, \code{heldout_rows}), \code{band}, \code{reps}, \code{demo} flag.
}
\description{
Decides, before outcome access, which estimands this design can deliver.
Simulated replicates are generated under the generate-treatment plasmode
design: covariate rows are resampled with replacement, treatment is drawn
from the propensity model fitted on the real data, and the outcome is drawn
from the prespecified simulated family of \code{\link[=support_surfaces]{support_surfaces()}}. On every
replicate the truth for each estimand (ATE, ATT among the treated as
realised, ATO, trimmed ATE on the prespecified band, matched ATT on the
matched set) is computed from the generating model, so the output is bias,
RMSE, coverage and SE calibration per estimand per grid point. Because the
matched, trimmed, ATT and ATO estimators run on the same replicates as the
full-cohort ATE, differences between them decompose into estimand
difference (their truths differ), efficiency loss (their empirical SDs
differ), and extrapolation error (only the ATE's bias grows along the
confounding axis when overlap is poor).
}
\section{What the map means}{

The gate reads the map. A cell passes when absolute bias is at most
\code{bias_tolerance} and coverage is at least \code{coverage_floor}. An estimand
whose estimator fails inside the plausible region of the grid is
infeasible for this design; which region is plausible is a subject-matter
judgement the design team records. The simulation never uses the real
treatment-outcome association unless \code{q0_source = "primary_outcome"} is
explicitly authorised.
}

\examples{
\dontrun{
lock <- create_simple_lock(sim_func1(400), "treatment", "event_24",
                           c("age", "sex", "biomarker"))
sim <- simulate_support(lock, reps = 100)
print(sim)
plot(sim)
}
}
\references{
Shaw PA et al. (2025) arXiv:2504.11740. Petersen ML et al.
(2012) Stat Methods Med Res 21:31-54. Li F, Morgan KL, Zaslavsky AM
(2018) JASA 113:390-400. Mao H, Li L, Greene T (2019) SMMR 28:2439-2454.
}
