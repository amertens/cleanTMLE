% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{checkpoint_balance}
\alias{checkpoint_balance}
\title{Check Point 2: Treatment Arm Comparability}
\usage{
checkpoint_balance(
  ps_diag,
  max_smd = 0.1,
  min_ess_pct = 50,
  stop_smd = 0.2,
  lock_hash = NA_character_
)
}
\arguments{
\item{ps_diag}{A \code{ps_diagnostics} object from
\code{\link{compute_ps_diagnostics}}.}

\item{max_smd}{Numeric; maximum tolerable absolute SMD after weighting.
Exceeding this (but not \code{stop_smd}) is a FLAG. Default: 0.10.}

\item{min_ess_pct}{Numeric; minimum ESS as a percent of the original N.
Default: 50.}

\item{stop_smd}{Numeric; a max weighted SMD strictly above this forces a
STOP rather than a FLAG. Default 0.20 (the former hard-coded floor).}

\item{lock_hash}{Character; optional lock hash for traceability.}
}
\value{
A \code{cleantmle_checkpoint} for Check Point 2.
}
\description{
Converts propensity-score diagnostics into a structured checkpoint
decision.  Evaluates standardised mean differences, effective sample
size, and overlap against user-specified thresholds.
}
\section{Clean-room stage}{
 Stage 2 (pre-outcome).
}

\examples{
dat  <- sim_func1(n = 500, seed = 1)
lock <- create_analysis_lock(dat, "treatment", "event_24",
                             c("age", "sex", "biomarker"), seed = 1)
ps   <- fit_ps_glm(lock)
diag <- compute_ps_diagnostics(ps)
cp2  <- checkpoint_balance(diag, lock_hash = lock$lock_hash)
print(cp2)

}
