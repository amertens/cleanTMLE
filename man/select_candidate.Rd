% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{select_candidate}
\alias{select_candidate}
\title{Select the Candidate Under the Locked Rule}
\usage{
select_candidate(
  results,
  rule = c("min_rmse", "min_max_rmse", "fiord_two_stage"),
  ...
)
}
\arguments{
\item{results}{A \code{\link[=stress_test]{stress_test()}} result (baseline rows are used for
the baseline rules; degraded rows for \code{"min_max_rmse"}). A legacy
\code{plasmode_results} object is also accepted.}

\item{rule}{One of \code{"min_rmse"}, \code{"min_max_rmse"},
\code{"fiord_two_stage"}.}

\item{...}{Passed to the internal selector (for example
\code{fiord_target_coverage}, \code{fiord_coverage_tol}, \code{thresholds}).}
}
\value{
The selected candidate specification (class
\code{tmle_selected_spec}), carrying the rule and the metrics at
selection; bind it to the lock with
\code{declare_estimand_ladder(candidate = )}.
}
\description{
Applies the prespecified selection rule to a stress-test result:
\code{"min_rmse"} (lowest RMSE on the clean-data baseline),
\code{"min_max_rmse"} (lowest worst-case RMSE across the declared
threats), or \code{"fiord_two_stage"} (the FIORD point-estimator stage:
screen candidates on oracle coverage, then take the lowest-SE
survivor).
}
\examples{
\dontrun{
best <- select_candidate(st, rule = "min_max_rmse")
lock <- declare_estimand_ladder(lock, primary = "ATE",
                                candidate = best)
}
}
