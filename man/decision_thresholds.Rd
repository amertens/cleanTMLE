% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleanroom.R
\name{decision_thresholds}
\alias{decision_thresholds}
\title{Construct a Prespecified Decision-Threshold Object}
\usage{
decision_thresholds(
  cohort_min_n_per_arm = 50L,
  cohort_min_events = 20L,
  cohort_min_prevalence = 0.01,
  cohort_stop_n_per_arm = 20L,
  cohort_stop_min_events = 20L,
  balance_max_smd = 0.1,
  balance_min_ess_pct = 50,
  balance_stop_smd = 0.2,
  plasmode_max_abs_bias = 0.01,
  plasmode_min_coverage = 0.9,
  plasmode_se_sd_window = c(0.8, 1.2),
  dq_max_abs_bias = 0.02,
  dq_min_coverage = 0.85,
  dq_max_rmse_ratio = 1.5,
  dq_flag_coverage = 0.9,
  dq_flag_rmse_ratio = 1.2,
  nco_alpha = 0.05,
  nco_rule = c("equivalence", "significance"),
  nco_null_band = 0.02,
  nco_adjust = c("none", "bonferroni")
)
}
\arguments{
\item{cohort_min_n_per_arm, cohort_min_events, cohort_min_prevalence}{Check Point 1 (cohort adequacy) FLAG thresholds.}

\item{cohort_stop_n_per_arm, cohort_stop_min_events}{Check Point 1 STOP
floors.}

\item{balance_max_smd, balance_min_ess_pct}{Check Point 2 (balance) FLAG
thresholds; \code{balance_stop_smd} the STOP floor.}

\item{balance_stop_smd}{Check Point 2 STOP floor on the max weighted SMD.}

\item{plasmode_max_abs_bias, plasmode_min_coverage, plasmode_se_sd_window}{Baseline-plasmode gate (\code{gate_check}) thresholds.}

\item{dq_max_abs_bias, dq_min_coverage, dq_max_rmse_ratio}{Check Point 2c
(\code{gate_dq}) STOP thresholds.}

\item{dq_flag_coverage, dq_flag_rmse_ratio}{Check Point 2c FLAG envelope.}

\item{nco_alpha, nco_rule, nco_null_band, nco_adjust}{Check Point 3
(negative-control / residual bias) thresholds. \code{nco_rule} is
\code{"equivalence"} (recommended) or \code{"significance"}.}
}
\value{
An object of class \code{cleantmle_thresholds}.
}
\description{
Bundles every GO / FLAG / STOP threshold used across the staged
workflow into one object so they are declared once, in one place, and
can be fingerprinted into the analysis lock with
\code{\link[=attach_decision_thresholds]{attach_decision_thresholds()}}. Each \verb{checkpoint_*()} / \verb{gate_*()}
function still accepts its thresholds as arguments; this object is the
single prespecified source those arguments should be drawn from, and
the helpers \code{\link[=dt_cohort]{dt_cohort()}}, \code{\link[=dt_balance]{dt_balance()}}, \code{\link[=dt_plasmode]{dt_plasmode()}}, \code{\link[=dt_dq]{dt_dq()}},
and \code{\link[=dt_nco]{dt_nco()}} extract the argument lists for each step.
}
\details{
Centralising the thresholds addresses a gap in earlier versions: the
decision rule — arguably the most outcome-relevant set of analyst
choices — was passed ad hoc to each checkpoint and was not part of the
lock fingerprint. With \code{\link[=attach_decision_thresholds]{attach_decision_thresholds()}} the thresholds
receive their own SHA-256 \code{thresholds_hash} recorded on the lock, so a
reviewer can verify the decision rule was fixed before unblinding.
}
\examples{
dt <- decision_thresholds(dq_max_abs_bias = 0.02, nco_rule = "equivalence",
                          nco_null_band = 0.02)
dt_dq(dt)
}
