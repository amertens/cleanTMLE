% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{declare_sensitivity_plan}
\alias{declare_sensitivity_plan}
\title{Declare a Sensitivity Analysis Plan}
\usage{
declare_sensitivity_plan(lock, label, description = NULL, settings = list())
}
\arguments{
\item{lock}{A \code{cleanroom_lock}.}

\item{label}{Character; short name for this sensitivity analysis.}

\item{description}{Character; what the sensitivity analysis evaluates.}

\item{settings}{A named list of parameters (e.g.
\code{list(truncation = c(0.01, 0.05, 0.10))}).}
}
\value{
Modified \code{cleanroom_lock} with appended sensitivity plan.
}
\description{
Attaches a named sensitivity analysis plan to the analysis lock.  The
plan is stored as metadata and does not alter the analytic specification
hash.  Multiple calls append additional plans.
}
\section{Clean-room stage}{
 Stage 1a (pre-outcome).
}

\examples{
dat  <- sim_func1(n = 200, seed = 1)
lock <- create_analysis_lock(dat, "treatment", "event_24",
                             c("age", "sex", "biomarker"), seed = 1)
lock <- declare_sensitivity_plan(lock,
  label       = "truncation_sensitivity",
  description = "Re-estimate under alternate PS truncation thresholds",
  settings    = list(truncation = c(0.01, 0.05, 0.10))
)
lock$sensitivity_plans

}
