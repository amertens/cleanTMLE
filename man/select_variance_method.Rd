% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance_methods.R
\name{select_variance_method}
\alias{select_variance_method}
\title{Select the variance method that achieves nominal oracle coverage (FIORD stage 2)}
\usage{
select_variance_method(
  plasmode_datasets,
  truth,
  treatment,
  outcome,
  covariates,
  estimator = c("tmle", "iptw"),
  methods = c("influence", "bootstrap"),
  target_coverage = 0.95,
  R_boot = 200L,
  sl_library = c("SL.glm"),
  truncate = 0.01
)
}
\arguments{
\item{plasmode_datasets}{A list of data frames (synthetic outcomes).}

\item{truth}{The synthetic-truth risk difference used to generate them.}

\item{treatment, outcome, covariates}{Column names.}

\item{estimator}{Locked point estimator: \code{"tmle"} or \code{"iptw"}.}

\item{methods}{Variance methods to compare. \code{"influence"} uses the
IF/IPTW analytic SE; \code{"bootstrap"} uses \code{\link[=bootstrap_rd_variance]{bootstrap_rd_variance()}}.}

\item{target_coverage}{Nominal coverage (default 0.95).}

\item{R_boot}{Bootstrap resamples per dataset for the bootstrap method.}

\item{sl_library}{SuperLearner library for TMLE.}

\item{truncate}{IPTW truncation.}
}
\value{
A list with \code{selected} (the chosen method), \code{coverage} (named
vector of empirical coverage by method), and \code{n_datasets}.
}
\description{
Given the locked point estimator and a set of synthetic (plasmode)
datasets with a known synthetic truth, evaluates each candidate variance
method by its empirical (oracle) coverage and returns the method whose
coverage is closest to the nominal target while not falling below it. This
is the second stage of the FIORD two-stage selector (Nance et al. 2026):
the point estimator is fixed, and only the variance method varies.
}
