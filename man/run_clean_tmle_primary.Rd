% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_tmle_staged.R
\name{run_clean_tmle_primary}
\alias{run_clean_tmle_primary}
\title{Primary Pass of the Staged Clean-Room Workflow}
\usage{
run_clean_tmle_primary(
  pre,
  authorization = NULL,
  allow_outcome_access = FALSE,
  truncate = 0.01,
  verbose = TRUE
)
}
\arguments{
\item{pre}{A \code{clean_tmle_preoutcome} object.}

\item{authorization}{A \code{pre_outcome_gate} token. If \code{NULL}, \code{pre$gate} is
used. The token must have \code{authorized = TRUE}, a \code{lock_hash} equal to
\code{pre$lock$lock_hash}, and an \code{audit_fingerprint} equal to the current
\code{pre$audit}.}

\item{allow_outcome_access}{Logical escape hatch; when \code{TRUE}, the token
checks are skipped and the outcome is read (a governance override). Default
\code{FALSE}.}

\item{truncate}{Propensity-score truncation for the primary TMLE.}

\item{verbose}{Logical; emit the estimate.}
}
\value{
A \code{clean_tmle_primary} object with the \code{tmle_result}, the
\code{risk_difference}, the CI, the (now authorised) \code{lock}, and the \code{dossier}.
}
\description{
Reads the observed outcome and runs the locked primary TMLE, but only after
verifying the pre-outcome authorisation. The authorisation (a
\code{pre_outcome_gate} from \code{\link[=authorize_outcome_analysis]{authorize_outcome_analysis()}}) must authorise the
analysis, match the lock hash, and match the current audit fingerprint;
otherwise the outcome is not read.
}
\seealso{
\code{\link[=run_clean_tmle_preoutcome]{run_clean_tmle_preoutcome()}}, \code{\link[=authorize_outcome_analysis]{authorize_outcome_analysis()}}
}
