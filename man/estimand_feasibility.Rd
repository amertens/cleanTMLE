% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand_ladder.R
\name{estimand_feasibility}
\alias{estimand_feasibility}
\title{Which Estimands Does This Design Support?}
\usage{
estimand_feasibility(
  ps_fit,
  estimands = .ladder_estimands,
  thresholds = NULL,
  trim_levels = c(0.05, 0.1),
  caliper_sd = 0.2,
  profile_vars = NULL
)
}
\arguments{
\item{ps_fit}{A \code{ps_fit}; untruncated scores are used.}

\item{estimands}{Subset of \code{c("ATE", "trimmed_ATE", "ATT", "ATC", "ATO", "matched_ATT")}.}

\item{thresholds}{A \code{\link[=support_thresholds]{support_thresholds()}} object.}

\item{trim_levels}{Trim levels reported for \code{trimmed_ATE}.}

\item{caliper_sd}{Caliper (SDs of the logit propensity) for
\code{matched_ATT}. Default 0.2.}
}
\value{
An object of class \code{estimand_feasibility}: a data.frame with one
row per estimand (and one per trim level), plus the thresholds.
}
\description{
One row per estimand: the weights it implies, their maximum and 99th
percentile, the effective sample size by arm under those weights, how many
patients it removes or leaves untouched, its target population, and the
graded verdict from the same thresholds as \code{\link[=assess_support]{assess_support()}}. The point
of the table is the contrast across rows: on a cohort with a severe
positivity violation the ATE's weights explode while the ATT's control
weights stay bounded by \verb{max g / (1 - g)} and the ATO's weights are
bounded by one by construction, so the design can support the second and
third where it cannot support the first.
}
\references{
Li F, Morgan KL, Zaslavsky AM (2018) JASA 113:390-400.
Crump RK et al. (2009) Biometrika 96:187-199.
}
